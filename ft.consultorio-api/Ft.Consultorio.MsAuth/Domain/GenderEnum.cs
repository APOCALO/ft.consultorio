namespace Ft.Consultorio.MsAuth.Domain
{
    // Género del usuario. Unspecified = el usuario no lo ha definido (campo opcional).
    public enum GenderEnum
    {
        Unspecified = 0,
        Male = 1,
        Female = 2
    }
}
